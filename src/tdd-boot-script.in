#!ipxe

echo tdd-boot-script @HOST@: ->
echo Press ctrl-c to exit iPXE auto boot
ifstat

@IF_CONFIG@ || shell
ifstat
route

kernel tftp://@TFTP_SERVER@/@HOST@/tdd-kernel
initrd tftp://@TFTP_SERVER@/@HOST@/tdd-initrd

imgstat

boot tdd-kernel initrd=tdd-initrd

shell
echo tdd-boot-script @HOST@: <-
